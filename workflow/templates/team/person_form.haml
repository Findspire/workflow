- extends "team/base.haml"

- block breadcrumbs
    {{ title }}

- block content
    %h1
        {{ title }}

    -if not creating
        %p
            %em
                %a{href: "{% url 'team:competence_instance_list' person_pk %}"}
                    Edit competences strength

    %form{action:"", method:"post"}
        - csrf_token
        {{ user_form.as_p }}
        {{ person_form.as_p }}
        %input{type:"submit", value:"{{ submit }}"}
