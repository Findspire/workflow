- extends "team/base.haml"

- block breadcrumbs
    {{ title }}

- block content
    %h1
        {{ title }}

    %form{action:"", method:"post"}
        - csrf_token
        {{ user_form.as_p }}
        {{ person_form.as_p }}
        %input{type:"submit", value:"{{ submit }}"}
